En el archivo custom_data.yaml se encuentran los
parámetros (clases) definidos en relación a las 
etiquetas manejadas en el conjunto de datos que 
se encuentra en el archivo tran_data.zip, el cual
se encuentra en formato YOLO.